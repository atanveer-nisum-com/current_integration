/**
 * State Of The User is mapped on this UserState interface
 * @author Shaharyar Iqbal
 */
export interface UserState {
    /**
     * Reflects current selected user's ,
     *
     */
    isLoged: boolean;
}
